@model advt.CMS.Models.ExamModel.ExamConfirmModel
@{
    Layout = null;
}

<head>
    <meta charset="UTF-8">
    @*<meta name="viewport" content="width=device-width, initial-scale=1.0">*@
    <meta name="viewport" content="width=375,initial-scale=1,minimum-scale=1,maximum-scale=1,user-scalable=no">
        <title>考试签到</title>
        <link href="~/Scripts/css/font-awesome.css" rel="stylesheet" />
        <link href="~/Scripts/elementUI/lib/theme-chalk/index.css" rel="stylesheet" />
        <link href="~/Scripts/css/exam.css" rel="stylesheet" />
        <style>
            @@media screen and (max-width:500px) {
             /*   .el-table td {
                    padding: 0px 0px;
                    font-size: 2px;
                }*/
            }
        </style>
</head>
<div id="app" style="margin-left:20px">
    <div v-if="isMobileTurn=='true'">
        @{            Layout = null;        }
    </div>
    <div>
        <template>
            @*<el-select v-model="TypeNames" placeholder="请选择考试类型">
                <el-option v-for="item in LExamType" :key="item.Key" :label="item.Value" :value="item.Key"></el-option>
            </el-select>
            <el-select v-model="DepartCodes" placeholder="请选择部门">
                <el-option v-for="item in LDepartCode" :key="item.Key" :label="item.Value" :value="item.Key"></el-option>
            </el-select>
            <el-button slot="append" @@click="SearchinByName">Search</el-button>*@

            <div style="width:100%;">
                <div style="margin-top:20px">
                    <el-page-header @@back="goBack" content="考试签到">
                </div>
                <div>
                    <el-table style="width: 100%;margin-top:20px " border :data="tableData">
                        <el-table-column type="index">
                        </el-table-column>
                        <el-table-column label="#" v-if="false"
                                         prop="ID">
                        </el-table-column>
                        <el-table-column label="区域"
                                         prop="OrgName">
                        </el-table-column>
                        <el-table-column label="部门"
                                         prop="DepartCode">
                        </el-table-column>
                        <el-table-column label="工号"
                                         prop="UserCode">
                        </el-table-column>
                        <el-table-column label="姓名"
                                         prop="UserName">
                        </el-table-column>
                        <el-table-column label="考试类型"
                                         prop="TypeName">
                        </el-table-column>
                        <el-table-column label="科目"
                                         prop="SubjectName">
                        </el-table-column>
                        @*<el-table-column label="是否签到" width="110">
                    <template slot-scope="scope">
                        <span v-if="scope.row.IsStartExam == false">未签到</span>
                        <span v-if="scope.row.IsStartExam == true">已签到</span>
                    </template>
                    </el-table-column>*@
                        <el-table-column label="操作">
                            <template slot-scope="scope">
                                <el-button size="mini" :disabled="scope.row.IsStartExam == true" @@click="Confirm(scope.row.ID)">签到</el-button>
                            </template>
                        </el-table-column>
                    </el-table>
                </div>
            </div>

        </template>

    </div>
        
    </div>
    <script src="~/Scripts/Vue/Vue.js"></script>
    <script src="~/Scripts/Vue/axios.js"></script>
    <script src="~/Scripts/elementUI/lib/index.js"></script>
    <script src="~/Scripts/Vue/polyfill.min.js"></script>
    <script type="text/javascript">
        new Vue({
            el: '#app',
            data: {
                dialogFormVisible: false,
                fullscreenLoading: false,
                form: [], 
                tableData: [],
                isMobileTurn: false,
                IsStartExam:false

            },
            mounted: function () {
                if (this.isMobile()) {
                    this.isMobileTurn = true
                } else {
                    this.isMobileTurn = false
                }
                this.getdata();
                this.aFun();
            },
            methods: {
                aFun() {
                    var docElement = document.documentElement;
                    var clientWidthValue = docElement.clientWidth > 750 ? 750 : docElement.clientWidth;
                    docElement.style.fontSize = 20 * (clientWidthValue / 375) + 'px';
                },
                isMobile: function () {
                    let flag = navigator.userAgent.match(/(phone|pad|pod|iPhone|iPod|ios|iPad|Android|Mobile|BlackBerry|IEMobile|MQQBrowser|JUC|Fennec|wOSBrowser|BrowserNG|WebOS|Symbian|Windows Phone)/i)
                    return flag
                },
                changeFun(val) {
                    var _this = this;
                    _this.checkBoxData = val;
                },
               
                handleclose() {
                    var _this = this;
                    _this.dialogUpdateVisible = false;
                },                
                Confirm(ID) {
                    var _this = this;
                    axios.post('/EPageMain/SaveConfirmUser', {
                        model: ID,
                    }).then(function (res) {
                        _this.tableData = res.data.tableData;
                    });
                },               
                changeDate: function (dates) {
                    var date = new Date(parseInt(dates.replace("/Date(", "").replace(")/", ""), 10));
                    Y = date.getFullYear() + '-';
                    M = (date.getMonth() + 1 < 10 ? '0' + (date.getMonth() + 1) : date.getMonth() + 1) + '-';
                    D = (date.getDate() < 10 ? '0' + (date.getDate()) : date.getDate()) + ' ';
                    h = (date.getHours() < 10 ? '0' + (date.getHours()) : date.getHours()) + ':';
                    m = (date.getMinutes() < 10 ? '0' + (date.getMinutes()) : date.getMinutes()) + ':';
                    s = (date.getSeconds() < 10 ? '0' + (date.getSeconds()) : date.getSeconds());
                    var NewDtime = Y + M + D + h + m + s;
                    var listDate = new Date(date);
                    var year = listDate.getFullYear();
                    var mon = listDate.getMonth() + 1;
                    var day = listDate.getDate();
                    return year + '-' + mon + '-' + day;
                },
                
                getdata: function () {
                    var _this = this;                   
                    axios.post('/EPageMain/ExamConfirmInfo', {
                    }).then(function (responese) {
                        _this.tableData = responese.data.tableData;
                    })
                },
                goBack: function () {
                var _this = this;
                if (_this.isMobileTurn == true) {
                    window.location.href = "/EPageMain/MaintainExam"
                } else {
                    window.location.href = "/EPageMain/MaintainExamPC"
                }
            },

            }

        })


    </script>

